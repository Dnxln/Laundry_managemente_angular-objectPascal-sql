import { MatRowSelectionDirective } from './mat-row-selection.directive';

describe('MatRowSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MatRowSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
