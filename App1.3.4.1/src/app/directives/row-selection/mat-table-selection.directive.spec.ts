import { MatTableSelectionDirective } from './mat-table-selection.directive';

describe('MatTableSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTableSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
